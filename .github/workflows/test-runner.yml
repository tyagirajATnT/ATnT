name: Simple Test

on: 
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Linux, X64, aws-codebuild]
    steps:
      - name: Simple Test
        run: |
          echo "Hello from runner"
          pwd
          ls -la
